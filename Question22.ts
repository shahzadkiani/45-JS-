// Define an array of colors
let colors: string[] = ["red", "blue", "green"];

// Try to access an index that is out of bounds (intentional error)
console.log(colors[3]); // Index 3 is out of bounds for this array

// To correct the error, we can access an existing index within the bounds
console.log(colors[2]); // This will print "green"



